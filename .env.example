# Frontend API base used by Vite at build-time
# Use /api/v1 when frontend is behind Nginx reverse proxy
VITE_API_BASE_URL=/api/v1

# Dev-only Vite proxy target (npm run dev)
VITE_API_PROXY_TARGET=http://localhost:8081
